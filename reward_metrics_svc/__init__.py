"""
reward_metrics_svc package.

This file intentionally contains minimal code so the service can be imported as a
package (e.g. `import reward_metrics_svc.main`) without relying on sys.path hacks.
"""

